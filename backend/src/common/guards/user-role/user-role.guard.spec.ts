import { UserRoleGuardTsGuard } from './user-role.guard.ts.guard';

describe('UserRoleGuardTsGuard', () => {
  it('should be defined', () => {
    expect(new UserRoleGuardTsGuard()).toBeDefined();
  });
});
